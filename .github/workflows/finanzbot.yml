name: Finanzbot Dashboard

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v4
        
      - name: 🐍 Python Setup
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          
      - name: 🛠️ Install Deps
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: 🤖 Bot Run
        run: python finanzbot.py
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: 💾 Update Dashboard (Commit & Push)
        run: |
          git config --global user.name "FinanzBot"
          git config --global user.email "bot@action.github"
          
          # HIER DER NEUE NAME:
          git add dashboard_finanzbot.html last_sent.json
          
          git diff-index --quiet HEAD || (git commit -m "📊 Dashboard Update" && git push)
